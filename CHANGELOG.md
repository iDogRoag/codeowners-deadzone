# Changelog

## 0.1.1

- Fix npm binary startup when `codeowners-deadzone` or `codz` is executed through npm's symlinked `bin` entries.

## 0.1.0

- Initial OSS version.
- Adds offline CODEOWNERS scanning, explanation, changed-file analysis, baseline support, and table, JSON, Markdown, HTML, and SARIF reports.
